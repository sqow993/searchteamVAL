name: Keep Render Alive
on:
  schedule:
    - cron: '*/5 * * * *'  # каждые 5 минут
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot
        run: curl -s https://searchteamval.onrender.com/healthcheck